# 📊 Telegram-бот для аналитики видео

Бот, который отвечает на вопросы о статистике видео на естественном русском языке и возвращает одно число.

---

## 🚀 Быстрый старт (без Docker)

### 1. Подготовка окружения

```bash
# Клонирование репозитория
git clone https://github.com/yourusername/video_analytics_bot.git
cd video_analytics_bot

# Создание виртуального окружения
python -m venv venv
source venv/bin/activate  # Linux/macOS
# или .\venv\Scripts\activate  # Windows

# Установка зависимостей
pip install -r requirements.txt

# Создание .env файла
cp .env.example .env
```

### 2. Настройка переменных окружения

Откройте `.env` и заполните:

```env
# Telegram Bot (получить через @BotFather)
TELEGRAM_BOT_TOKEN=

# Database (PostgreSQL должен быть запущен локально или через Docker)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=video_analytics

# YandexGPT (получить в Яндекс Облаке)
YANDEXGPT_API_KEY=
YANDEXGPT_FOLDER_ID=
```

---

## 🗄️ Запуск базы данных (PostgreSQL)

### Вариант A: Локальная установка PostgreSQL

1. Создайте базу данных `video_analytics` и пользователя `postgres`
2. Выполните SQL-скрипт создания таблиц:
   ```bash
   psql -h localhost -U postgres -d video_analytics -f sql/01_create_tables.sql
   ```

### Вариант B: Запуск PostgreSQL через Docker (рекомендуется)

```bash
# Запуск только PostgreSQL в фоне
docker run -d \
  --name video_analytics_db \
  -e POSTGRES_DB=video_analytics \
  -e POSTGRES_USER=postgres \
  -e POSTGRES_PASSWORD=postgres \
  -p 5432:5432 \
  -v $(pwd)/sql:/docker-entrypoint-initdb.d \
  postgres:15-alpine

# Проверка готовности (~30 секунд ожидания)
sleep 30
```

---

## 📥 Загрузка данных из JSON

Поместите файл `videos.json` в папку `data/`, затем выполните:

```bash
python scripts/load_json.py
```

Скрипт автоматически:
- Прочитает `data/videos.json`
- Создаст записи в таблицах `videos` и `video_snapshots`
- Покажет количество загруженных записей

---

## 🤖 Как задать токен Telegram-бота

1. Найдите в Telegram `@BotFather`
2. Отправьте команду `/newbot`
3. Следуйте инструкциям:
   - Введите имя бота (например: `Video Analytics RL Bot`)
   - Введите юзернейм (должен оканчиваться на `bot`, например: `video_analytics_rl_test_bot`)
4. Скопируйте полученный токен вида:
   ```
   1234567890:AAHjKLmnOpQrStUvWxYzAbCdEfGhIjKlMnO
   ```
5. Вставьте токен в `.env`:
   ```env
   TELEGRAM_BOT_TOKEN=1234567890:AAHjKLmnOpQrStUvWxYzAbCdEfGhIjKlMnO
   ```

---

## ▶️ Запуск бота

```bash
python bot/main.py
```

Бот начнёт принимать сообщения в Telegram!

---

## 🧠 Архитектура и подход к преобразованию запросов

### Общая схема работы

```
Пользователь → Telegram → aiogram → YandexGPT → SQL → PostgreSQL → Ответ (число)
```

### Ключевые компоненты

| Компонент | Технология | Назначение |
|-----------|------------|------------|
| Telegram Bot | aiogram 3.12 | Приём и отправка сообщений |
| База данных | PostgreSQL + asyncpg | Хранение статистики видео |
| LLM | YandexGPT API | Преобразование текста в SQL |
| Обработка дат | Регулярные выражения | Нормализация дат с русского на ISO |

### Преобразование текста в SQL: 3-этапный подход

#### Этап 1: Нормализация дат на русском языке
```python
# Пример преобразования:
"28 ноября 2025" → "2025-11-28"
"с 1 по 5 ноября 2025" → "с 2025-11-01 по 2025-11-05"
```
Реализовано через регулярные выражения в `bot/utils/llm.py::_normalize_dates()`.

#### Этап 2: Генерация SQL через YandexGPT
- Промпт содержит **полную схему БД** с описанием полей и типов
- Чёткие **правила выбора таблиц**:
  - `videos` — для итоговой статистики («сколько видео», «набрало просмотров»)
  - `video_snapshots` — для динамики («на сколько выросло», «прирост»)
- Примеры запросов для снижения галлюцинаций
- `temperature: 0.0` — минимизация творческих отклонений

#### Этап 3: Безопасное выполнение запроса
- Асинхронное выполнение через `asyncpg`
- Извлечение только первого значения из результата
- Обработка ошибок без раскрытия деталей пользователю

---

## 🤖 Промпт для YandexGPT

Полный промпт (сокращённая версия для документации):

```
"""Ты — система аналитики видео. Преобразуй вопрос пользователя в ОДИН корректный SQL-запрос на PostgreSQL.

СХЕМА БАЗЫ ДАННЫХ:

Таблица "videos" (итоговая статистика по каждому видео):
- id (BIGINT) — идентификатор видео
- creator_id (TEXT) — идентификатор креатора (всегда строка в формате UUID, например: '8b76e572635b400c9052286a56176e03')
- video_created_at (TIMESTAMP) — дата публикации видео
- views_count (INTEGER) — финальное количество просмотров
- likes_count (INTEGER) — финальное количество лайков
- comments_count (INTEGER) — финальное количество комментариев
- reports_count (INTEGER) — финальное количество жалоб

Таблица "video_snapshots" (почасовые замеры статистики):
- id (BIGSERIAL) — идентификатор снапшота
- video_id (BIGINT) — ссылка на видео
- views_count (INTEGER) — текущие просмотры на момент замера
- likes_count (INTEGER) — текущие лайки на момент замера
- comments_count (INTEGER) — текущие комментарии на момент замера
- reports_count (INTEGER) — текущие жалобы на момент замера
- delta_views_count (INTEGER) — прирост просмотров с прошлого замера
- delta_likes_count (INTEGER) — прирост лайков с прошлого замера
- delta_comments_count (INTEGER) — прирост комментариев с прошлого замера
- delta_reports_count (INTEGER) — прирост жалоб с прошлого замера
- created_at (TIMESTAMP) — время замера (раз в час)

ВАЖНЫЕ ПРАВИЛА:
1. ВСЕГДА указывай таблицу явно для всех полей:
   - Для creator_id используй: videos.creator_id
   - Для времени замеров используй: video_snapshots.created_at
   - Для даты публикации видео используй: videos.video_created_at
2. Когда речь идет о "промежутке времени", "часах", "замерах" — ВСЕГДА используй video_snapshots.created_at
3. creator_id — это СТРОКА, поэтому ВСЕГДА заключай его в одинарные кавычки
4. В ответе должен быть ТОЛЬКО один SQL-запрос без лишнего текста
5. Запрос должен возвращать РОВНО ОДНО ЧИСЛО

ПРИМЕРЫ:

Вопрос: "Сколько видео опубликовал креатор с id 8b76e572635b400c9052286a56176e03 в период с 1 ноября 2025 по 5 ноября 2025?"
SQL: SELECT COUNT(*) FROM videos WHERE creator_id = '8b76e572635b400c9052286a56176e03' AND video_created_at::date BETWEEN '2025-11-01' AND '2025-11-05';
Вопрос: "На сколько просмотров суммарно выросли все видео креатора с id 123 в промежутке с 10:00 до 15:00 28 ноября 2025 года?"
SQL: SELECT SUM(delta_views_count) FROM video_snapshots JOIN videos ON video_snapshots.video_id = videos.id WHERE videos.creator_id = 123 AND DATE(video_snapshots.created_at) = '2025-11-28' AND video_snapshots.created_at::time BETWEEN '10:00:00' AND '15:00:00';

Вопрос: {user_query}
SQL:"""
```

### Почему этот подход работает

| Проблема | Решение в промпте |
|----------|-------------------|
| Галлюцинации | `temperature: 0.0` + примеры с правильными запросами |
| Неправильный выбор таблицы | Чёткие правила: «сколько видео» → `videos`, «прирост» → `video_snapshots` |
| Ошибки с датами | Нормализация на русском языке до отправки в LLM |
| Лишний текст в ответе | Требование «ТОЛЬКО один SQL-запрос» + постобработка регулярками |

---

## 🧪 Примеры запросов

Бот корректно отвечает на:

| Запрос | Тип ответа |
|--------|------------|
| `Сколько всего видео есть в системе?` | Общее количество (например: `4281`) |
| `Сколько видео набрало больше 100000 просмотров за всё время?` | Счётчик видео с условием |
| `На сколько просмотров в сумме выросли все видео 28 ноября 2025?` | Сумма `delta_views_count` за дату |
| `Сколько разных видео получали новые просмотры 27 ноября 2025?` | `COUNT(DISTINCT video_id)` с фильтром |

> ⚠️ **Важно:** Бот возвращает **только число**, без дополнительного текста.

---

## 🔍 Отладка

### Проверка подключения к БД
```bash
# Если PostgreSQL запущен через Docker
docker exec -it video_analytics_db psql -U postgres -d video_analytics -c "SELECT COUNT(*) FROM videos;"

# Если PostgreSQL локальный
psql -h localhost -U postgres -d video_analytics -c "SELECT COUNT(*) FROM videos;"
```

### Тестирование промпта без запуска бота
```bash
python test_pipeline.py
```

### Просмотр логов бота
При локальном запуске все логи видны в консоли:
```bash
python bot/main.py
```

---

## 📁 Структура проекта

```
video_analytics_bot/
├── bot/
│   ├── main.py              # Точка входа
│   ├── handlers/
│   │   └── bot_handler.py   # Обработка сообщений Telegram
│   └── utils/
│       ├── database.py      # Асинхронная работа с PostgreSQL
│       ├── llm.py           # Клиент YandexGPT + промпт
├── sql/
│   └── 01_create_tables.sql # Схема базы данных
├── scripts/
│   └── load_json.py         # Загрузка данных из JSON
├── data/
│   └── videos.json          # Файл с данными (не включён в репозиторий)
├── requirements.txt         # Зависимости Python
├── docker-compose.yml       
└── README.md                # Этот файл
```

---



Запуск проверки:
```
/check @ваш_бот https://github.com/ваш_юзернейм/video_analytics_bot
```
